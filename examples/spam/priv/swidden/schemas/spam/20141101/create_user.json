{
    "description": "Create a swidden user. A user has 'username' and 'password'.",
    "properties": {
        "username": {"type": "string", "required": true},
        "password": {"type": "string", "required": true}
    }
}
